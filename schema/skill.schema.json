{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/voxstudio-me/voxstudio-skills/blob/main/schema/skill.schema.json",
  "title": "VoxStudio Pro skill frontmatter",
  "description": "The YAML frontmatter at the top of a SKILL.md file.",
  "type": "object",
  "required": ["name", "description"],
  "additionalProperties": true,
  "properties": {
    "name": {
      "type": "string",
      "minLength": 1,
      "maxLength": 80,
      "description": "Short human name for the skill."
    },
    "description": {
      "type": "string",
      "minLength": 1,
      "maxLength": 1024,
      "description": "One line: what it does and when to use it. The agent matches on this."
    }
  }
}
